# 서버 이전/환경 분리 시 이 값만 바꾸면 됨 (미설정 시 socket.js의 fallback 값 사용)
VITE_SERVER_URL=https://fifa-ddojumgum-server.onrender.com

# GA4 측정 ID (G-XXXXXXXXXX). 미설정 시 GA 비활성 — 로컬 개발(dev 모드)에서는 설정해도 전송 안 함
VITE_GA_MEASUREMENT_ID=
